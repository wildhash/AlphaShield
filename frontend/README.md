# Alpha Shield — Frontend (Vite + React + TypeScript)

This folder contains a starter frontend for the Alpha Shield prototype (landing + dashboard).

How to run
1. cd frontend
2. npm install
3. npm run dev

Files included
- index.html, src/, styles/, vite config, tsconfig. Uses React, React Router and React Query.

Environment
- For live data connect to a backend API (see server examples in the conversation). Set VITE_API_BASE to your API base.
